#include "main.h"
/**
 * _strcpy - copies string pointed to by src,
 * plus the terminating null byt (\0),
 * to buffr pointed to by dest.
 * @dest: value to be valuate.
 * @src: value to be evaluate.
 * Return: dest, a char *.
 */
char *_strcpy(char *dest, char *src)
{
	strcpy(dest, src);
	return (dest);
}
