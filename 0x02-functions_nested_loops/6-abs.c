#include "main.h"
/**
 * _abs -compute the absolute value of integer
 * @a: the value that function return absolute nummber
 * Description: return required results
 * Return: return integer value 0
 */
int _abs(int a)
{
	if (a >= 0)
		return (a);
	else
		return (a * -1);
	return (0);
}
