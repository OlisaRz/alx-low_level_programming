#include "main.h"
/**
 * _islower - checks for lower case characters
 * @c: the character to check out
 * Description: checks for lowercase character.
 * Return: 1 if c is lower case. 0 otherwise
 * **/
int _islower(int c)
{
	return (c >= 'a' && c <= 'z');
}
